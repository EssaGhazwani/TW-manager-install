{
  "name": "tw-manager-install-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Parse and behavior tests for the XKWDStore Agent Protocol-v2 Windows installer",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}